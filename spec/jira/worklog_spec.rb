require 'spec_helper'

describe Jira::Worklog do
  it 'has a version number' do
    expect(Jira::Worklog::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
